services:
  breast-cancer-decision-support:
    build: .
    container_name: breast-cancer-decision-support
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data:ro
      - ./models:/app/models
      - ./reports:/app/reports
      - ./logs:/app/logs
    environment:
      - PYTHONPATH=/app
    command: streamlit run app/Home.py --server.address=0.0.0.0 --server.port=8501

